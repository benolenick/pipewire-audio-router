[Unit]
Description=PipeWire Audio Router — per-app sink routing
After=pipewire.service wireplumber.service

[Service]
ExecStart=%h/.local/bin/audio-router.sh
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
